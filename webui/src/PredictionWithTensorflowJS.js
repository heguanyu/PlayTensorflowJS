import tfjs from '@tensorflow/tfjs'

export default class PredictionWithTensorflowJS {
    getScore(model, features) {
        // Use TFJS to predict the model vs features
        return 0.0;
    }
}
